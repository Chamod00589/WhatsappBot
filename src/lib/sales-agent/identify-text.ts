export function identifyRejectText(useSinglish: boolean): string {
  return useSinglish
    ? 'Hari — venath photo ekak ho bag name eka send karanna.'
    : 'Okay — please send another photo or the bag name.'
}
